mutation CreateCustomer($data: CreateCustomerInput!) {
  createCustomer(data: $data) {
    id
    name
    lastName
    phone
  }
}

query Customers {
  customers {
    id
    lastName
    name
    phone
    updatedAt
    createdAt
  }
}